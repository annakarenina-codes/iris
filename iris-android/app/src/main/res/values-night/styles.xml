<resources>
    <!-- Mirrors the day theme; only the window surface and the status-bar icon color
         flip, so a system-dark cold start opens on the night background instead of white. -->
    <style name="AppTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorAccent">#7C3AED</item>
        <item name="android:windowBackground">#12101a</item>
    </style>
</resources>
