# Gradle / Android build outputs
.gradle/
build/
app/build/

# Machine-specific config — local.properties carries iris.accessToken (SECRET)
local.properties

# IDE
.idea/
*.iml
captures/

# Nested tool metadata
.atl/
